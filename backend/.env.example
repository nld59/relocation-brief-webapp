OPENAI_API_KEY=YOUR_KEY_HERE
OPENAI_MODEL=gpt-5

# Optional: separate smaller model for QA
OPENAI_QA_MODEL=gpt-4o-mini

# Verified lookup (official sources via Tavily)
TAVILY_API_KEY=YOUR_TAVILY_KEY_HERE

# Comma-separated allowlist of official domains for verified lookup.
# If omitted, the backend uses a safe default for Brussels/Belgium.
OFFICIAL_DOMAINS=brussels.be,be.brussels,environnement.brussels,ibsa.brussels,statbel.fgov.be
